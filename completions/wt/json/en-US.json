{
    "-p": "{{$json_info.p}}",
    "-d": "{{$json_info.d}}",
    "sp": "✨{{$json_info.sp}}",
    "sp -p": "{{$json_info.p}}",
    "sp -d": "{{$json_info.d}}",
    "-M": "✨Launch window in maximized mode",
    "-M -p": "{{$json_info.p}}",
    "-M -d": "{{$json_info.d}}",
    "-M sp": "✨{{$json_info.sp}}",
    "-M sp -d": "{{$json_info.d}}",
    "-F^up": "✨Launch window in full screen mode",
    "-F^up -p": "{{$json_info.p}}",
    "-F^up -d": "{{$json_info.d}}",
    "-F^up sp": "✨{{$json_info.sp}}",
    "-F^up sp -d": "{{$json_info.d}}",
    "-f": "✨Launch window in Focus mode",
    "-f -p": "{{$json_info.p}}",
    "-f -d": "{{$json_info.d}}",
    "-f sp": "✨{{$json_info.sp}}",
    "-f sp -d": "{{$json_info.d}}",
    "--pos": "Specify the location of the terminal in the format \"x,y\"",
    "--size": "Specify the number of columns and rows for the terminal in the format \"column,row\"",
    "-w": [
        "Specify the terminal window for running the given command line\n",
        "\"0\" always refers to the current window"
    ],
    "-v": [
        "--version\n",
        "Display the version"
    ],
    "-h": [
        "--help\n",
        "Print this help message and exit"
    ],
    "wt_core_info": {
        "p": "✨-p <profile>\nStart the terminal with the specified configuration file\ne.g. -p \"Window PowerShell\" --- Start with Window PowerShell",
        "d": "-d <start_dir>\nSpecify the startup directory\n例如: -d \"D:\\\"  ---  Start from D drive",
        "sp": "split-pane\nCreate a new split pane"
    }
}
