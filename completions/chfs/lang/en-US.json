{
    "--path=": [
        "{{$PSCompletions.config.sym_opt}}Directories where store shared files, separated by '|'. (default \".\")\n",
        "e.g. chfs --path=D:\\\n",
        "     chfs --path=\"C:\\Program Files\""
    ],
    "--port=": [
        "{{$PSCompletions.config.sym_opt}}HTTP listening port(Default is 80)\n",
        "eg: chfs --port=8080"
    ],
    "--file=": "{{$PSCompletions.config.sym_opt}}A configuration file which overwrites the settings",
    "--version": "Show application version",
    "--help": [
        "Show context-sensitive help.\n",
        "(also try --help-long and  --help-man)"
    ]
}
